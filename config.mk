# config.mk - MakeSilicon Configuration File

# Parent directory where projects are created
PROJ_DIR := ip_repos

# Directories to create (comma-separated)
# Uncomment and modify the line below to customize directories
# DIRECTORIES := src,sim,synth,docs,scripts,testbench,constraints,lib,mem,logs,results

# Initialize git repository by default (yes/no)
# Uncomment and set to 'yes' to enable git initialization
GIT_INIT := yes

# File containing the list of repositories
REPO_LIST := repos.txt